class Patient():
def __init__(self, last_name):
    pid4344 = Patient("Anand")
  print(pid4344.last_name)


class enter_class:
 def __init__(student,age):
    student.age = age
    
 def mystudent(student):
    if student.age < 21:
     print(student.age)
        
x = enter_class(10)
x.mystudent()

class patient:
 def __init__(self, name, age, fullname):
    self.name = name
    self.age = age
    self.fullname = fullname
 def minor(self):
    print("the patient is always good" + self.name + self.fullname)
 def change_last(self, new_fullname):
    self.fullname = new_fullname
    print("the patient is always good" + self.fullname)
y = patient("l" , 21 ,"khan"  )
# y = patient("pol" , 16 ,"hjsdahkjas"  )
# z = minor()
y.change_last("lol")


class matric_class:
  def __init__(intro, name, marks):
     intro.name = name
     intro.marks = marks
  
  def Myfun(intro):
        print("hello your name is " + intro.name + " Hello Your age Is " + intro.marks)
    
m = matric_class("python" , "100") 
m.Myfun()


class students:
  def __init__(user,naam, umer):
    user.naam = naam
    user.umer = umer
    
L = students("Furqan" , 55)
print(L.naam)
print(L.umer)


class Person:
  def __init__(self, name, age):
    self.name = name
    self.age = age


p1 = Person("MaaZ" , 25)

print(p1.name)
print(p1.age)

class LClass:
    y = 20
    l = 40
    k = 1500
    m = 2254
    print(y , l , k , m)
    
Maaz = LClass()
print(Maaz.y)

class MyClass:
  x = 5

print(MyClass)
p1 = MyClass()
print(p1.x)

  def calc(value1, value2):
        total = value1 + value2
        print(total)

    calc(32,132)

    def names_of_couple():
    add2 = 20
    add1 = 12
    total = add1 + add2
    print(total)
names_of_couple()

classGIRLS = ["laiba", "rameen", "sumaika", "mehreen", "humna" , "salik"]
classBOYS = "salik"
for x in classGIRLS:
 if x == classBOYS:
    print("nice couple")

    def cpu():
         ram = 2
         rom = 4
         harddisk = 1
         body = 2
         lol = ram + body
print(lol)
cpu()  
def calc(firstNum , secNum , thirdNum):
    total = firstNum+secNum+thirdNum
    print(total)
calc(10,12,33) 

def imtiaz(firstName , lastName , data = "aic018778"):
    total = firstName + " " + lastName + " " + data
    print(total)
imtiaz("maaz" , "furqan")    

cleanest = ("new york", "dubai", "tucson", "lahore", "istambol")
userInput = input("")
for z in cleanest:
    if userInput == z:
        print("ya field kaaaam krna walo ki hai")

class Patient():
def __init__(self, last_name):
    pid4344 = Patient("Anand")
  print(pid4344.last_name)


   def company(you_are_a_great_employe):
    print(company)
company("you_are_a_great_employe")


def calculator(addOne, addTwo, addThree, addfour):
    add = addOne + addTwo + addThree + addfour
    print(add)
calculator(10, 20, 30, 40)
calculator(50, 60, 70, 80)
calculator(1, 2, 3, 4)

